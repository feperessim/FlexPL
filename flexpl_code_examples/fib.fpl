define int fib(int n) {
       if (n equal 0) {
       	  return 0;
       }
       else if (n equal 1) {
       	    return 1;
       }
       else {
       
       
	int a = fib(k);
	int b =  fib(n - 2);
	return a + b;
       }
}
	   